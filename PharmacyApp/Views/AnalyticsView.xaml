<UserControl x:Class="PharmacyApp.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style TargetType="Border" x:Key="KpiCardStyle">
            <Setter Property="Background" Value="{DynamicResource CardBgBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Width" Value="220"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <TextBlock Text="Аналитика" Style="{StaticResource WindowTitleStyle}" DockPanel.Dock="Left"/>
            <Button Style="{StaticResource IconButtonStyle}" DockPanel.Dock="Right"
                    Command="{Binding RefreshCommand}" ToolTip="Обновить">
                <TextBlock Text="↻" FontSize="16" FontWeight="Bold"/>
            </Button>
        </DockPanel>

        <WrapPanel Grid.Row="2" Margin="0,0,0,8" Orientation="Horizontal">
            <Border Style="{DynamicResource KpiCardStyle}" Margin="0,0,16,8">
                <StackPanel>
                    <TextBlock Text="Выручка сегодня" Style="{StaticResource SecondaryTextStyle}"/>
                    <TextBlock Text="{Binding TodayRevenue, StringFormat='#,##0.00 ₽'}" Style="{StaticResource PriceStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource KpiCardStyle}" Margin="0,0,16,8">
                <StackPanel>
                    <TextBlock Text="Чеков сегодня" Style="{StaticResource SecondaryTextStyle}"/>
                    <TextBlock Text="{Binding TodaySalesCount}" Style="{StaticResource PriceStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource KpiCardStyle}" Margin="0,0,16,8">
                <StackPanel>
                    <TextBlock Text="Истекает за 30 дней" Style="{StaticResource SecondaryTextStyle}"/>
                    <TextBlock Text="{Binding ExpiringCount}" Style="{StaticResource PriceStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource KpiCardStyle}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Выручка за месяц" Style="{StaticResource SecondaryTextStyle}"/>
                    <TextBlock Text="{Binding MonthRevenue, StringFormat='#,##0.00 ₽'}" Style="{StaticResource PriceStyle}"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <TextBlock Text="Выручка за 7 дней" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart Series="{Binding RevenueSeries}"
                                        LegendLocation="None"
                                        Margin="0,28,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding RevenueLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis LabelFormatter="{Binding RevenueFormatter}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <TextBlock Text="Топ-5 препаратов" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart Series="{Binding TopDrugsSeries}"
                                        LegendLocation="None"
                                        Margin="0,28,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding TopDrugsLabels}"/>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <TextBlock Text="Остатки по категориям" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart Series="{Binding CategoryColumnSeries}"
                                        LegendLocation="None"
                                        Margin="0,28,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding CategoryColumnLabels}" />
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <TextBlock Text="Эффективность сотрудников" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart Series="{Binding StaffPerformanceSeries}"
                                        LegendLocation="None"
                                        Margin="0,28,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding StaffLabels}" />
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>