<UserControl x:Class="PharmacyApp.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PharmacyApp.Views"
             xmlns:conv="clr-namespace:PharmacyApp.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:ExpiryToBrushConverter x:Key="ExpiryToBrush"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <TextBlock Text="Склад" Style="{StaticResource WindowTitleStyle}" VerticalAlignment="Center"/>
            <Button Style="{StaticResource IconButtonStyle}" DockPanel.Dock="Right"
                    Command="{Binding LoadCommand}" ToolTip="Обновить">
                <TextBlock Text="↻" FontSize="16" FontWeight="Bold"/>
            </Button>
        </DockPanel>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DockPanel>
                <CheckBox Content="Показать истекающие в течение 30 дней (и с остатком > 0)"
                          IsChecked="{Binding OnlyExpiring30}"/>
                <TextBlock DockPanel.Dock="Right" Style="{StaticResource SecondaryTextStyle}"
                           Text="Подсветка сроков: 30 дней"/>
            </DockPanel>
        </Border>

        <DataGrid Grid.Row="4" ItemsSource="{Binding Batches}" IsReadOnly="True">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{Binding ExpiryDate, Converter={StaticResource ExpiryToBrush}, ConverterParameter=30}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Товар" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Партия" Binding="{Binding BatchNumber}" Width="160"/>
                <DataGridTextColumn Header="Годен до" Binding="{Binding ExpiryDate, StringFormat=d}" Width="120"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>